<%= render partial: "shared/nav" %>

<%= render partial: 'form', locals: { method: :post, url: merchant_bulk_discounts_path(@merchant, @discount), class: 'merchant_bulk_discount_form', local: true, model: nil } %>
